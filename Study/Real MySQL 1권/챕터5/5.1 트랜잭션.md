# 트랜잭션
- 트랜잭션은 작업의 완전성을 보장해 주는 것
- 처리하지 못할 경우에는 원 상태로 복구해서 작업의 일부만 적용되는 현상이 발생하지 않게 만들어주는 기능
- 잠금과 다른 점: 잠금은 동시성을 제어하기 위한 기능, 트랜잭션은 데이터의 정합성을 보장해주기 위한 기능
	- 잠금은 여러 커넥션에서 동시에 동일한 자원을 요청할 경우 순서대로 한 시점에는 하나의 커넥션만 변경할 수 있게 해주는 역할을 한다.
- 격리 수준이라는 것은 하나의 트랜잭션 내에서 또는 여러 트랜잭션 간의 작업 내용을 어떻게 공유하고 차단할 것인지를 결정하는 레벨을 의미한다.

# 5.1.1 MySQL 에서의 트랜잭션
MyISAM이나 MEMORY 스토리지 엔진이 InnoDB 보다 더 빠르다고 생각할 수 있지만 사실은 MyISAM이나 MEMORY 스토리지 엔진 같이 트랜잭션을 지원하지 않는 엔진이 빠르지도 않고, 더 많은 고민거리를 만들어낸다.
이유:

- 트랜재겻ㄴ은 꼭 여러 개의 변경 작업을 수행하는 쿼리가 조합됐을 때만 의미 있는 개념은 아니다.
- 트랜잭션은 쿼리가 하나든 여러개든 논리적인 작업 셋 자체가 100% 적용되거나 아무것도 적용되지 않아야 함을 보장해주는 것이다.
- 예시) auto commit mode에서 insert 여러개 했을 때 일부 데이터가 중복이면 duplicate 에러가 나는데 myisam은 부분 데이터가 insert 되어 있는 상태이고, 